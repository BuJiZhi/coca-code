import React from 'react';
import './Animate.css';
import { useSpring, animated } from 'react-spring';

const Animate: React.FC = () => {
  return (
    <div className="animateRoot">
      animate
    </div>
  );
}

export default Animate;
